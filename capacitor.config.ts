import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bookishboy.app',
  appName: 'bookstore',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
